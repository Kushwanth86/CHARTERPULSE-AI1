import type { DecisionResponse } from "../api";
import type { ProcurementContext } from "./NewProcurement";
export default function ActualOutcomes({ procurement, decision }: { procurement: ProcurementContext | null; decision: DecisionResponse | null }) {
  return <div className="page-stack"><div className="page-heading"><div><span className="eyebrow">GOVERNANCE / OUTCOMES</span><h1>Actual Outcomes</h1><p>Post-voyage performance is separated from prediction. The page will only show actuals after a voyage outcome is ingested.</p></div><span className="data-badge proxy">NO FABRICATION</span></div><section className="enterprise-card"><div className="outcome-banner"><div><span className="eyebrow">ACTIVE PROCUREMENT</span><h2>{procurement ? `${procurement.cargo.material} · ${procurement.cargo.quantity_mt.toLocaleString()} MT` : "No active procurement"}</h2><p>{procurement ? `${procurement.originPort.name} → ${procurement.destinationPort.name}` : "Create a procurement to establish a decision/outcome key."}</p></div><div className="outcome-status"><span>ACTUAL FREIGHT</span><strong>NOT INGESTED</strong></div><div className="outcome-status"><span>TRANSIT DELAY</span><strong>NOT INGESTED</strong></div><div className="outcome-status"><span>DEMURRAGE</span><strong>NOT INGESTED</strong></div></div></section><section className="enterprise-card"><div className="table-wrap"><table><thead><tr><th>Decision run</th><th>Predicted freight P50</th><th>Actual freight</th><th>Delay</th><th>Demurrage</th><th>Outcome status</th></tr></thead><tbody><tr><td>{decision?.decision_run_id || "—"}</td><td>{decision ? `$${decision.forecast_p50.toFixed(2)}/MT` : "—"}</td><td>Pending actual voyage data</td><td>Pending</td><td>Pending</td><td><span className="data-badge proxy">PENDING INGESTION</span></td></tr></tbody></table></div></section></div>;
}
